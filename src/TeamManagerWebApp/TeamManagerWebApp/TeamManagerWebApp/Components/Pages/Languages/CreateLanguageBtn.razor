@inject NavigationManager NavigationManager

<button class="btn btn-primary mb-3" @onclick="CreateLanguage">Add New Language</button>

@code {
    private void CreateLanguage()
    {
        NavigationManager.NavigateTo("/edit-language");
    }
}
